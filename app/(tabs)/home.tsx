import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {

    return (
        <SafeAreaView className="bg-background flex-1">
            
        </SafeAreaView>
    );
};

export default Home;
